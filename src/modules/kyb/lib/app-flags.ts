// Temporal: modo local forzado para demo interna sin backend.
// Cuando se reconecte la API, cambiar ambos flags a false o eliminar esta rama.
export const SHOULD_USE_DEMO_PREFILL = true;
export const SHOULD_USE_LOCAL_ONBOARDING = true;
